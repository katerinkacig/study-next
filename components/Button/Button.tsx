import {ButtonProps} from "./Button.props";import styles from "./Button.module.css";import clsx from "clsx";function Button({mod, children, className, ...props} : ButtonProps): JSX.Element {    return (        <button            className={clsx(className, styles.button, mod==="primary" && styles.primary, mod==="secondary" && styles.secondary)}            {...props}        >            {children}        </button>    );}export default Button;